<div class="container">
<h2>お気に入り</h2>
<div class="my-2">
<%= link_to "戻る", user_path(current_user) %>
</div>

<% if @posts.present? %>
  <%= render 'public/posts/list', posts: @posts %>
<% else %>
  <%= flash.now[:notice] %>
<% end %>

</div>