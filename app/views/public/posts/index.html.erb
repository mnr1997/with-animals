<div class="container">
  <h2>投稿一覧</h2>

  <%= form_with url: posts_path, method: :get do |f| %>
    <%= f.text_field :caption, value: params[:caption] %>
    <%= f.submit "検索" %>
  <% end %>

  <%= link_to "カテゴリー検索", categories_path %>

  <%= render 'list', posts: @posts %>
</div>